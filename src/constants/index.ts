export { default as StorageKey } from './StorageKey';

const BASE_URL = '';
const MEDIA_URL = '';
const ACCESS_TOKEN = '';

export const DEFAULT = {
  baseUrl: BASE_URL,
  mediaUrl: MEDIA_URL,
  accessToken: ACCESS_TOKEN,
  accountId: 0,
  region: 'US',
  language: 'en-US',
};
